# Guía de Despliegue

Esta guía describe los pasos necesarios para desplegar la aplicación web contenerizada utilizando Docker y Docker Compose.

---

## 1. Requisitos previos

Asegúrate de tener instalados:

- Docker
- Docker Compose
- Git (opcional)

Verifica la instalación:

```bash
docker --version
docker compose version
